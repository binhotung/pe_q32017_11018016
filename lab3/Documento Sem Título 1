#include <stdio.h>



/*programa recursivo, que faz somatório*/

unsigned int SOM(unsigned int n)
{
	if(n==1) return 1;
	return n +SOM(n-1);
}

int main ()
{
	unsigned int n;
	scanf("%d", &n);
	printf("a Somatória é: %d\n",SOM(n));

	return 0;
}
